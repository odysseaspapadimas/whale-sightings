import { Icon } from '@iconify/react';
import markerIcon from '@iconify/icons-mdi/fish';

const Marker = () => {
    return (
        <div>
            <Icon icon={markerIcon} className="marker-icon"/>
        </div>
    )
}

export default Marker
